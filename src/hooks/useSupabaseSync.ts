// import { useEffect } from 'react';
// import { useGameStore } from '../store/gameStore';
// import { supabase } from '../lib/supabase';

// Sync Hook Disabled (Local Storage Only)
export function useSupabaseSync() {
    // Disabled
}
